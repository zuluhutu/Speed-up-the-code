{
  "name": "Speed-up-the-code",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "settings": {},
  "extensions": [
    "ms-vscode.powershell"
  ],
  // Install and import modules
  "postCreateCommand": "pwsh -Command \"Install-Module -Name PSFramework, ImportExcel, Profiler -Force -Scope AllUsers; Import-Module PSFramework; Import-Module ImportExcel; Import-Module Profiler\"",
  "remoteUser": "vscode"
}